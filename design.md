# Design Rules

- Blue (both the light-blue background and blue text) should be used primarily for hover effects, not for the selected state.
  - Example: for the selected state, use a bolder font-weight (550) + `text-primary` color instead of blue.
